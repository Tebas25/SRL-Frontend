// DTO para el reporte de rutas por provincia
export interface RutasPorProvincia {
  provincia: string;
  totalRutas: number;
}

// DTO para el reporte de rutas por conductor
export interface RutasPorConductor {
  conductor: string;
  totalRutas: number;
}
